---
phase: 07
plan: 07-01
type: execute
wave: 2
depends_on:
  - 06-03
files_modified:
  - src/styles.css
  - src/client/components/EventBoard.tsx
  - src/client/components/AdminPanel.tsx
  - src/main.tsx
  - src/client/components/ui-correctness.test.tsx
autonomous: true
requirements: [UI-09, UI-10]
user_setup: []
must_haves:
  truths:
    - Action buttons do not overflow/overlap/bleed their frames across Events/Manager/Admin views at desktop and at narrow widths (<=980px and <=680px).
    - Admin audit log scrolls within its panel at narrow widths while the audit header + refresh action remain visible/accessible.
    - CI-style gates remain green: `npm test` and `npm run build` pass without adding major UI dependencies.
  artifacts:
    - src/styles.css
    - src/client/components/EventBoard.tsx
    - src/client/components/AdminPanel.tsx
    - src/client/components/ui-correctness.test.tsx
  key_links:
    - from: src/styles.css
      to: ".audit-panel / .section-title-row / .audit-list"
      via: "audit scroll containment selectors"
      pattern: "\\.audit-panel|\\.section-title-row|\\.audit-list"
    - from: src/client/components/ui-correctness.test.tsx
      to: "EventBoard/AdminPanel DOM hooks"
      via: "JSDOM structural assertions for UI-09/UI-10"
      pattern: "querySelector\\(|getByText\\(|\\.audit-list|\\.action-row|\\.manage-row"
---

<objective>
Fix UI correctness regressions (button layout + admin audit scroll containment) with minimal-surface CSS/layout changes and a small deterministic regression test.
</objective>

<tasks>
<task id="07-01-01" type="auto">
  <name>CSS-first layout fixes for UI-09 + UI-10</name>
  <files>src/styles.css</files>
  <read_first>src/styles.css, src/client/components/EventBoard.tsx, src/client/components/AdminPanel.tsx, src/main.tsx, .planning/phases/07-ux-polish-ui-correctness/07-CONTEXT.md</read_first>
  <action>
    Implement CSS/layout changes with minimal surface area (per D-01) to address:

    UI-09 (buttons overflow/overlap/bleed frames):
    - Ensure flex/grid children can shrink without forcing overflow by applying `min-width: 0` to relevant layout children:
      - `.action-row button`, `.manage-row > *`, `.section-title-row > *`, `.admin-list-row > *`, and any other button container that can overflow.
    - Harden `.manage-row` grid against mid-width layouts (between 680px and 980px):
      - Keep first column flexible (`minmax(0, 1fr)`) and allow action columns to shrink (`minmax(0, ...)`), avoiding `auto` columns that refuse to shrink.
    - Prevent frame bleed in dense rows by ensuring container overflow handling is intentional (avoid `overflow: visible` on tight card/panel headers).

    UI-10 (audit log scrolls within panel; header/actions remain accessible):
    - Convert `.audit-panel` into a 2-row grid (header row + list row) and constrain its height on narrower screens using a viewport-based max height (e.g. `max-height: calc(100vh - <topbar+shell padding>)`).
    - Make `.audit-list` the scroll container: `overflow: auto`, `min-height: 0`, `overscroll-behavior: contain` so it scrolls inside the panel.
    - Ensure `.section-title-row` (audit header/actions) remains in the non-scrolling header area.

    Validate behavior at the existing breakpoints (<=980px and <=680px) and ensure no new UI framework/library dependencies are introduced (per constraints).
  </action>
  <verify><automated>npm run build</automated></verify>
  <done>
    UI-09 + UI-10 are fixed via CSS changes in `src/styles.css`: no overflow/overlap/bleed in action button areas and audit list scrolls inside its panel with header/actions accessible at narrow widths.
  </done>
</task>

<task id="07-01-02" type="auto">
  <name>Minimal markup hook adjustments if CSS alone can’t express the contract</name>
  <files>src/client/components/EventBoard.tsx, src/client/components/AdminPanel.tsx, src/main.tsx</files>
  <read_first>src/client/components/EventBoard.tsx, src/client/components/AdminPanel.tsx, src/main.tsx, src/styles.css, .planning/phases/07-ux-polish-ui-correctness/07-CONTEXT.md</read_first>
  <action>
    Only if needed after Task 07-01-01 (per D-01):
    - Add or adjust a minimal, semantic wrapper/class to make scrolling and shrink behavior explicit without refactoring logic.
      Examples (choose only what’s necessary):
      - Wrap the audit list in a dedicated container with a class such as `.audit-scroll` so CSS can target the scroll region deterministically.
      - Add a class to a problematic header/action cluster so it can opt into `min-width: 0` and wrapping behavior without global rules.
    - Do not alter fetch/data logic, and do not add new dependencies.
  </action>
  <verify><automated>npm run build</automated></verify>
  <done>
    Any markup changes are limited to structural hooks (classes/wrappers) needed for layout correctness; core component behavior remains unchanged and the UI correctness issues are resolved.
  </done>
</task>

<task id="07-01-03" type="auto">
  <name>Add deterministic Vitest regression for UI structure hooks</name>
  <files>src/client/components/ui-correctness.test.tsx</files>
  <read_first>src/client/components/EventBoard.tsx, src/client/components/AdminPanel.tsx, src/client/api/request.ts, src/styles.css, .planning/phases/07-ux-polish-ui-correctness/07-CONTEXT.md</read_first>
  <action>
    Add a small, deterministic regression test (per D-03) that runs under Vitest without Playwright:
    - Create `src/client/components/ui-correctness.test.tsx` using JSDOM (`@vitest-environment jsdom`).
    - Mock `requestJson` so components can render without network.
    - Stub `globalThis.EventSource` to avoid real SSE connections.

    Assertions should be DOM-structure “contracts” that support UI-09/UI-10 fixes:
    - `EventBoard` renders action button containers with the expected class hooks:
      - For a loaded open event, `.action-row` exists and contains the two participation buttons.
      - For a manageable open event, `.manage-row` exists and contains the start input + action buttons (count/labels).
    - `AdminPanel` renders the audit log section with stable hooks:
      - A section labeled `Audit-Log` exists.
      - Inside it, `.section-title-row` exists (header/actions) and `.audit-list` exists (the scroll container target).

    Keep the test deterministic: fixed timestamps/IDs in fixtures, no timers except flushing microtasks, and no reliance on computed CSS (JSDOM doesn’t compute layout).
  </action>
  <verify><automated>npm test</automated></verify>
  <done>
    A deterministic Vitest regression exists and passes locally/CI, ensuring the UI structure hooks needed for UI-09/UI-10 remain stable while also keeping `npm run build` green.
  </done>
</task>
</tasks>

<verification>
- [ ] `npm test`
- [ ] `npm run build`
</verification>

<output>
After completion, create `.planning/phases/07-ux-polish-ui-correctness/07-01-SUMMARY.md`
</output>


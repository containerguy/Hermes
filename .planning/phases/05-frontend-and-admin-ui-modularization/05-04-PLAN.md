---
phase: 05
plan: 05-04
type: execute
wave: 4
depends_on:
  - 05-03
files_modified:
  - src/main.tsx
  - src/client/components/AdminPanel.tsx
autonomous: true
requirements: [UI-04, UI-05, UI-06, UI-07, UI-08]
user_setup: []
must_haves:
  truths:
    - Admin area is extracted into modules while preserving current UX and responsive behavior.
  artifacts:
    - src/client/components/AdminPanel.tsx
---

<objective>
Extract admin modules and preserve responsive navigation/layout.
</objective>

<tasks>
<task id="05-04-01" type="auto">
  <name>Extract AdminPanel into module and keep admin UX behavior stable</name>
  <files>src/main.tsx, src/client/components/AdminPanel.tsx</files>
  <read_first>src/main.tsx, src/styles.css</read_first>
  <action>
    Move `AdminPanel` into `src/client/components/AdminPanel.tsx`, keep existing fetch patterns and UI sections (users, settings, storage, rate-limits, invites, audit).
    Ensure responsive layout remains unchanged.
  </action>
  <verify><automated>npm test && npm run build</automated></verify>
  <done>Admin UI is modularized without behavior drift.</done>
</task>
</tasks>

<output>
After completion, create `.planning/phases/05-frontend-and-admin-ui-modularization/05-04-SUMMARY.md`
</output>


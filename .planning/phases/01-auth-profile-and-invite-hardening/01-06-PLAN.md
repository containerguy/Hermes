---
phase: 01
plan: 01-06
type: execute
wave: 7
depends_on:
  - 01-01
  - 01-02
  - 01-03
  - 01-04
  - 01-05
  - 01-07
  - 01-08
files_modified:
  - src/server/http/app-flow.test.ts
  - .env.example
  - readme.md
  - building.md
  - .planning/phases/01-auth-profile-and-invite-hardening/01-VALIDATION.md
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06, AUTH-07, PROF-01, PROF-02, PROF-03, INV-01, INV-02, INV-04, INV-05, INV-06, INV-07]
user_setup: []
must_haves:
  truths:
    - Every Phase 1 requirement has automated validation or explicit documented manual rationale.
    - New Phase 1 operational settings and invite-code credential behavior are documented.
    - `npm test`, `npm run build`, and `npm audit --omit=dev` are run before Phase 1 is marked complete.
    - Later-phase work remains deferred and is not claimed by Phase 1.
  artifacts:
    - src/server/http/app-flow.test.ts
    - .env.example
    - readme.md
    - building.md
    - .planning/phases/01-auth-profile-and-invite-hardening/01-VALIDATION.md
  key_links:
    - Validation rows must match the revised 01-01 through 01-08 task IDs.
    - Docs must reflect the actual env vars used by 01-07, especially `HERMES_CSRF_SECRET` if implemented.
    - INV-03, event capacity, backup/restore safety, PWA reliability, and frontend extraction remain later phases.
---

<objective>
Finish Phase 1 with full validation, release-relevant docs, and explicit scope boundaries.

Purpose: prevent a false-positive release state after the hardening work lands.
Output: complete test coverage map, docs/env updates, and final validation commands.
</objective>

<threat_model>
High severity threats block completion:
- T-01 false green: build passes while Phase 1 security behaviors lack tests.
- T-02 undocumented secret: new CSRF/session settings are missing from operator docs.
- T-03 scope overclaim: Phase 1 accidentally claims INV-03 concurrency or later release phases.
- T-04 stale validation: `01-VALIDATION.md` no longer matches the actual task IDs.
- T-05 production handoff risk: final commands are skipped or failed without documented blockers.
</threat_model>

<tasks>
<task id="01-06-01" type="auto">
  <name>Complete Phase 1 API regression coverage</name>
  <files>src/server/http/app-flow.test.ts</files>
  <read_first>.planning/phases/01-auth-profile-and-invite-hardening/01-VALIDATION.md, src/server/http/app-flow.test.ts, .planning/REQUIREMENTS.md</read_first>
  <action>Review all tests added in 01-01 through 01-05 and fill gaps for every Phase 1 requirement. The suite must cover enumeration resistance, login request throttling, OTP verify throttling, challenge superseding/cleanup, token hashing, session invalidation after role/admin email/delete, CSRF missing/valid paths, active email uniqueness, display-name update, email-change request/verify failure/success, default device names, session rename ownership, invite entropy/defaults, invite lifecycle endpoints, invite registration throttling, rate-limit admin API, and audit secret redaction. Keep tests deterministic with `HERMES_DEV_LOGIN_CODE = "123456"` and Supertest agents.</action>
  <acceptance_criteria>
    - Tests contain cases or descriptions for `generic login`, `rate limit`, `session token`, `csrf`, `email change`, `device`, and `invite lifecycle`.
    - Tests assert audit metadata does not contain a full invite code.
    - Tests assert duplicate active email returns `email_existiert_bereits`.
    - Tests assert missing CSRF fails and valid CSRF succeeds.
    - `npm test -- --run src/server/http/app-flow.test.ts` passes.
  </acceptance_criteria>
  <verify><automated>npm test -- --run src/server/http/app-flow.test.ts</automated></verify>
  <done>Focused API coverage exists for every Phase 1 requirement and high-risk behavior.</done>
</task>

<task id="01-06-02" type="auto">
  <name>Document Phase 1 operational changes</name>
  <files>.env.example, readme.md, building.md</files>
  <read_first>.env.example, readme.md, building.md, src/server/auth/csrf.ts, src/server/auth/sessions.ts, src/server/http/admin-routes.ts</read_first>
  <action>Update only release-relevant docs/env notes caused by Phase 1. If `HERMES_CSRF_SECRET` exists in code, add it to `.env.example`, `readme.md`, and `building.md` with placeholder value guidance and no real secret. If no additional secret exists, document the local default and production recommendation accurately. Add a short note that invite codes are credential-like, generated by Hermes, visible to admins, and intentionally masked in audit metadata. Do not expand backup/restore, PWA, Docker, or broad release checklist content beyond Phase 1 changes.</action>
  <acceptance_criteria>
    - `.env.example` mentions every new Phase 1 env var that exists in code.
    - `readme.md` says invite codes are credential-like and audit logs mask them.
    - `building.md` documents any new required or optional Phase 1 secret.
    - Docs contain no real secrets and use placeholders such as `change-me`.
    - `npm run build` passes.
  </acceptance_criteria>
  <verify><automated>npm run build</automated></verify>
  <done>Operators can configure and understand Phase 1 security behavior without secret leakage.</done>
</task>

<task id="01-06-03" type="auto">
  <name>Run final Phase 1 validation and confirm scope boundaries</name>
  <files>.planning/phases/01-auth-profile-and-invite-hardening/01-VALIDATION.md, src/server/http/app-flow.test.ts</files>
  <read_first>.planning/phases/01-auth-profile-and-invite-hardening/01-VALIDATION.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, package.json</read_first>
  <action>Run the final Phase 1 command set: `npm test`, `npm run build`, and `npm audit --omit=dev`. If a command fails due to an environment blocker, document the blocker in the execution summary instead of marking the plan complete silently. Confirm Phase 1 implementation and summaries do not claim INV-03 atomic invite `maxUses` concurrency, event capacity concurrency, backup/restore safety, PWA reliability, or broad frontend extraction. Update `01-VALIDATION.md` execution status only after commands have actually run during execution.</action>
  <acceptance_criteria>
    - `npm test` exits 0 or the environment blocker is documented.
    - `npm run build` exits 0 or the environment blocker is documented.
    - `npm audit --omit=dev` exits 0 or the environment blocker is documented.
    - Any mention of `INV-03` in Phase 1 execution notes says it is deferred to Phase 2.
    - `01-VALIDATION.md` still maps every revised task ID.
  </acceptance_criteria>
  <verify><automated>npm test && npm run build && npm audit --omit=dev</automated></verify>
  <done>Phase 1 has been validated end to end or has explicit documented blockers.</done>
</task>
</tasks>

<verification>
- [ ] `npm test`
- [ ] `npm run build`
- [ ] `npm audit --omit=dev`
</verification>

<success_criteria>
- All Phase 1 requirement IDs are covered by revised plan frontmatter and validation rows.
- Automated tests cover the release-critical auth, profile, invite, audit, CSRF, migration, and UI request behavior.
- Docs/env reflect only Phase-1-relevant operational changes.
- Later-phase work remains explicitly deferred.
- Final validation commands are run or blockers are documented.
</success_criteria>

---
phase: 03
plan: 03-03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/server/http/admin-routes.ts
  - src/server/storage/s3-storage.ts
  - src/server/audit-log.ts
  - src/main.tsx
  - readme.md
  - building.md
  - src/server/http/app-flow.test.ts
autonomous: true
requirements: [BKP-05, BKP-06]
user_setup: []
must_haves:
  truths:
    - Backup and restore audit entries identify actor and outcome without secrets or misleading partial-success metadata (BKP-05, D-13, D-14).
    - Audit logging is best-effort and never blocks backup/restore primary actions (constraint).
    - Admin UI shows backup status, restore success recovery info, and restore failure diagnostics without adding a dedicated new status endpoint (D-01, D-02, D-11).
    - Operator docs contain a short runbook: backup verification, validation-first restore model, failed-restore recovery steps, S3 config checklist, and single-writer warning (BKP-06, D-15).
  artifacts:
    - src/server/http/admin-routes.ts
    - src/main.tsx
    - readme.md
    - building.md
  key_links:
    - Audit events use `tryWriteAuditLog()` so failures do not block primary operations.
    - UI renders diagnostics from restore response body (not just error code) while keeping stable German error codes.
---

<objective>
Wire audit, UI, and operator docs around the safer backup/restore behavior.

Purpose: satisfy BKP-05/BKP-06 by making backup/restore auditable and operator-safe end-to-end, including UI visibility and a short runbook with rollback steps.
Output: best-effort audit coverage, admin UI surface for status/diagnostics/recovery IDs, and documentation updates.
</objective>

<tasks>
<task id="03-03-01" type="auto">
  <name>Add complete best-effort audit coverage for backup/restore and storage config checks</name>
  <files>src/server/http/admin-routes.ts, src/server/storage/s3-storage.ts, src/server/audit-log.ts, src/server/http/app-flow.test.ts</files>
  <read_first>.planning/phases/03-backup-and-restore-safety/03-CONTEXT.md, src/server/http/admin-routes.ts, src/server/audit-log.ts, src/main.tsx, src/server/http/app-flow.test.ts</read_first>
  <action>
    Implement D-13 audit events (best-effort; must not block primary actions):
    - Backup:
      - `storage.backup_start`
      - `storage.backup_success`
      - `storage.backup_failed`
    - Restore:
      - `storage.restore_start`
      - `storage.restore_validated`
      - `storage.restore_recovery_created` (include recovery id + key, non-secret location details)
      - `storage.restore_completed`
      - `storage.restore_failed`
    - Storage config checks:
      - `storage.config_check` logged at the start of backup/restore, capturing:
        - backend enabled/disabled
        - bucket/key/region/endpoint
        - credential source presence (env vars set? creds file configured?) but NEVER the secret values (D-13, D-14)

    Requirements for audit metadata and summary:
    - MUST NOT include secrets, SDK headers, or stack traces (D-14).
    - MUST NOT claim completion on partial state; only log `*_completed` after restore transaction commits (BKP-05).
    - Prefer concise German summaries like existing patterns in `admin-routes.ts` (e.g. “Admin hat Restore validiert.”).

    Enforce D-14 by construction:
    - Never pass raw `error` objects into audit metadata.
    - Build audit metadata using **explicit allowlisted key/value picks** only (strings/numbers/booleans), and apply **string truncation** (e.g. max 240 chars) to any field derived from exceptions.
    - If you include an error hint in audit metadata, it must come from a helper like `toSafeStorageErrorHint(err)` that explicitly forbids `stack` and headers and avoids credential-like substrings.

    Ensure every audit write uses `tryWriteAuditLog(context, ...)` (never `writeAuditLog`) so audit failures do not block backup/restore (constraint).

    Add at least one focused assertion to `src/server/http/app-flow.test.ts` that the audit log contains restore/backup audit actions after calling the endpoints (do not require S3; use the current behavior where backup succeeds even without S3 enabled, and for restore you can assert start/failed actions when restore fails).
  </action>
  <verify><automated>npm test -- --run src/server/http/app-flow.test.ts</automated></verify>
  <done>
    - Audit log shows start/validated/completed/failed for restore and success/failure for backup.
    - Audit metadata is safe and does not contain secrets or raw headers.
    - Audit write failures do not prevent backup/restore responses.
  </done>
</task>

<task id="03-03-02" type="auto">
  <name>Admin UI wiring for status + restore diagnostics + recovery ID display</name>
  <files>src/main.tsx, src/server/http/admin-routes.ts</files>
  <read_first>.planning/phases/03-backup-and-restore-safety/03-CONTEXT.md, src/main.tsx, src/server/http/admin-routes.ts</read_first>
  <action>
    Wire the UI to display the Phase 3 safety outputs (D-01, D-02, D-11):
    - Backup status panel:
      - Show last success/failure timestamps.
      - Show failure code/category and human-readable summary.
      - Show non-secret location details (bucket/key/region/endpoint).
      - If storage backend is disabled, show a muted note that S3 snapshot storage is disabled.
    - Restore UX:
      - Keep the confirmation prompt, but clarify it reflects “validation-first + pre-restore recovery snapshot”.
      - On successful restore:
        - Display returned recovery info: `recovery.id` and `recovery.key` (D-08).
        - Display a short hint: “Rollback: lade recovery key aus S3 und nutze Restore erneut.”
      - On restore failure:
        - Continue to show the stable error message from `errorMessages.restore_fehlgeschlagen`.
        - Additionally, if the caught error is an `ApiError`, extract `caught.body.diagnostics` and render it in a compact operator panel:
          - Summary string
          - Missing tables, column mismatches, and FK failures (cap lists to avoid huge UI)
          - Migration mismatch signal (snapshotLatest vs liveLatest)
        - Do not render raw error objects; display only the safe diagnostics fields.
    Ensure UI keeps last-known backup status even after a backup failure (D-03) by not clearing status state on error.

    This plan should not introduce any new fetch endpoints. It should reuse:
    - existing `loadAdminData()` calls and the extended admin settings payload from 03-01
    - existing `POST /api/admin/restore` response body from 03-02
  </action>
  <verify><automated>npm test && npm run build</automated></verify>
  <done>
    - Admin UI displays backup status and restore diagnostics/recovery info.
    - Restore failures show actionable diagnostics without leaking secrets (D-14).
    - No new dedicated status endpoint exists (D-01).
  </done>
</task>

<task id="03-03-03" type="auto">
  <name>Update operator documentation runbook (readme + building)</name>
  <files>readme.md, building.md</files>
  <read_first>.planning/phases/03-backup-and-restore-safety/03-CONTEXT.md, readme.md, building.md, src/server/storage/s3-storage.ts, src/main.tsx</read_first>
  <action>
    Per D-15, update BOTH `readme.md` and `building.md` with a short operator runbook section covering:
    - **Single-writer warning**: Hermes uses SQLite + S3 snapshots; running multiple writing instances is unsafe.
    - **How to verify backups** in the Admin UI:
      - Where the status panel is shown
      - What “last success” and “last failure” mean
      - Non-secret location details shown
    - **Restore safety model**:
      - Validation-first (tables, migrations, columns, foreign keys)
      - Hard-blocked on validation failure (no force restore)
      - All-or-nothing restore
      - Pre-restore recovery snapshot creation and retention (keep last N recoveries)
    - **Failed-restore recovery steps**:
      - Use the returned `recovery.id` / `recovery.key`
      - How to retrieve the recovery sqlite from S3 (show an example `aws s3 cp ...` command using `s3://bucket/recoveries/...`)
      - How to roll back: place the recovery snapshot at the configured `HERMES_S3_DB_KEY` (or temporarily change `HERMES_S3_DB_KEY`) and run restore again
      - Mention that restore will validate before applying
    - **S3 configuration checklist**:
      - `HERMES_STORAGE_BACKEND=s3`
      - bucket/region/endpoint/key env vars
      - credential sources supported: env vars or `HERMES_S3_CREDS_FILE`
      - explicitly remind that `s3.creds` is not committed

    Keep the section short and practical. Avoid repeating full `.env` blocks that already exist; link to the existing S3 env section and add only the runbook steps.
  </action>
  <verify><automated>npm test && npm run build</automated></verify>
  <done>
    - Docs contain an operator-facing runbook matching D-15 with clear failed-restore recovery steps.
    - Single-writer snapshot warning is explicit.
  </done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → audit log storage | Audit persistence must not block operator actions. |
| server → client diagnostics | Diagnostics shown in UI must be safe and bounded. |
| docs → operator actions | Runbook must not encourage unsafe multi-writer behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-03-08 | I | Audit metadata & UI diagnostics | mitigate | Only include safe fields; omit secrets/headers/stack traces; cap lists (D-14). |
| T-03-09 | R | Audit reliability | accept | Best-effort audit writes can fail; do not block primary operations; log audit failure server-side. |
| T-03-10 | S/T | Operator misunderstandings | mitigate | Docs emphasize single-writer model + validation-first restore + recovery key rollback steps (D-15). |
</threat_model>

<verification>
- [ ] `npm test`
- [ ] `npm run build`
- [ ] Focused: `npm test -- --run src/server/http/app-flow.test.ts`
</verification>

<success_criteria>
- BKP-05: Backup/restore audit entries capture actor and outcome without secrets or partial-success confusion.
- BKP-06: `readme.md` and `building.md` include a short runbook with failed-restore recovery steps and single-writer warning.
- Admin UI provides operator visibility into status, diagnostics, and recovery IDs without new dedicated status endpoints.
</success_criteria>

<output>
After completion, create `.planning/phases/03-backup-and-restore-safety/03-03-SUMMARY.md`
</output>


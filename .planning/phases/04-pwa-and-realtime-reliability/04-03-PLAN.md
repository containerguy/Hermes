---
phase: 04
plan: 04-03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/server/realtime/event-bus.ts
  - src/server/http/realtime-routes.ts
  - src/main.tsx
  - README.md
  - building.md
autonomous: true
requirements: [PWA-04]
user_setup: []
must_haves:
  truths:
    - SSE realtime updates remain resilient across idle connections and reconnect conditions.
    - Documentation notes proxy timeout expectations and heartbeat behavior.
  artifacts:
    - src/server/realtime/event-bus.ts
    - src/main.tsx
  key_links:
    - Phase 4 context D-09..D-11 in 04-CONTEXT.md.
---

<objective>
Add SSE heartbeat/reconnect resilience and document proxy timeout expectations.

Purpose: satisfy PWA-04 by keeping realtime event updates working across idle proxy disconnects, while keeping polling as a safe fallback.
Output: server heartbeat + retry hints, client reconnect loop, and small docs update.
</objective>

<tasks>
<task id="04-03-01" type="auto">
  <name>Add server-side SSE retry + heartbeat and client reconnect loop</name>
  <files>src/server/realtime/event-bus.ts, src/server/http/realtime-routes.ts, src/main.tsx</files>
  <read_first>.planning/phases/04-pwa-and-realtime-reliability/04-CONTEXT.md, src/server/realtime/event-bus.ts, src/server/http/realtime-routes.ts, src/main.tsx</read_first>
  <action>
    Implement PWA-04 with Phase 4 decisions D-09..D-11:

    - In `src/server/realtime/event-bus.ts`:
      - Emit `retry: 15000` once per connection (before first event).
      - Start a heartbeat timer per client (e.g. every 25s) that sends `event: heartbeat` with `{ at }`.
      - Ensure heartbeat timers are cleared on client unregister (avoid leaks).

    - In `src/main.tsx` `EventBoard`:
      - Replace the one-shot `EventSource` creation with a small reconnect helper:
        - On `onerror`, close the source and recreate it after a backoff (e.g. 1s, 2s, 4s up to 15s).
        - Keep `liveState` transitions: `connecting` -> `live`, and switch to `polling` only after an error (but continue trying SSE reconnect).
      - Keep the existing 30s polling interval as a fallback.
      - Handle `heartbeat` event (optional) to keep `liveState` accurate without reloading.
  </action>
  <acceptance_criteria>
    - SSE connections receive periodic heartbeat events and a retry hint.
    - Client reconnects SSE after errors without requiring a page reload.
    - Polling fallback remains intact.
  </acceptance_criteria>
  <verify><automated>npm test && npm run build</automated></verify>
  <done>Realtime updates remain resilient across idle connections and reconnects.</done>
</task>

<task id="04-03-02" type="auto">
  <name>Document heartbeat and proxy timeout expectations</name>
  <files>README.md, building.md</files>
  <read_first>README.md, building.md, .planning/ROADMAP.md</read_first>
  <action>
    Add a short note to both `README.md` and `building.md` explaining:
    - Hermes realtime uses SSE with heartbeats.
    - Reverse proxies/load balancers can still time out idle connections; Hermes will reconnect, and polling acts as a fallback.
    Keep the section concise and operator-oriented.
  </action>
  <acceptance_criteria>
    - Docs mention SSE heartbeat and reconnect/polling expectations.
  </acceptance_criteria>
  <verify><automated>npm run build</automated></verify>
  <done>Operators have guidance about realtime behavior and proxy timeouts.</done>
</task>
</tasks>

<verification>
- [ ] `npm test`
- [ ] `npm run build`
</verification>

<success_criteria>
- PWA-04 is met: realtime event updates remain resilient across idle proxy connections or reconnects.
</success_criteria>

<output>
After completion, create `.planning/phases/04-pwa-and-realtime-reliability/04-03-SUMMARY.md`
</output>


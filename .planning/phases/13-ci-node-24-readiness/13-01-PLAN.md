---
phase: 13-ci-node-24-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/docker-image.yml
  - .planning/codebase/INTEGRATIONS.md
autonomous: false
requirements:
  - CI-01
tags:
  - ci
  - github-actions
  - docker
  - node-24
user_setup: []

must_haves:
  truths:
    - "All GitHub Actions used in .github/workflows/docker-image.yml are pinned to major versions documented as Node-24-compatible by their maintainers (actions/checkout@v5, actions/setup-node@v5, docker/setup-buildx-action@v4, docker/login-action@v4, docker/metadata-action@v6, docker/build-push-action@v7)."
    - "The workflow sets the env FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true at workflow level so any latent Node 20-only action would fail the PR now instead of in June 2026."
    - "On a PR branch run of .github/workflows/docker-image.yml, the verify job passes npm ci, npm test, npm run build, and npm audit --omit=dev, and the docker job builds the image (push is PR-skipped, which is expected per the existing if: condition)."
    - ".planning/codebase/INTEGRATIONS.md reflects the new pinned action versions under the 'GitHub Actions And Container Registry' section (replacement, not duplicate)."
    - "Any new high/critical npm audit findings introduced by no other change than the action version bump are surfaced in the PR description / phase summary (D-05), not silently masked."
  artifacts:
    - path: ".github/workflows/docker-image.yml"
      provides: "CI workflow pinned to Node-24-compatible action versions with FORCE_JAVASCRIPT_ACTIONS_TO_NODE24 env"
      contains: "FORCE_JAVASCRIPT_ACTIONS_TO_NODE24"
    - path: ".planning/codebase/INTEGRATIONS.md"
      provides: "Documentation of pinned action versions + CI Node 24 Migration note"
      contains: "CI Node 24 Migration"
  key_links:
    - from: ".github/workflows/docker-image.yml"
      to: "GitHub Actions runner (Node 24 JS action runtime)"
      via: "action pins + FORCE_JAVASCRIPT_ACTIONS_TO_NODE24 env"
      pattern: "FORCE_JAVASCRIPT_ACTIONS_TO_NODE24:\\s*(?:\"true\"|true)"
    - from: ".planning/codebase/INTEGRATIONS.md"
      to: ".github/workflows/docker-image.yml"
      via: "documented pinned versions match workflow"
      pattern: "actions/checkout@v5|docker/build-push-action@v7"

verification_criteria:
  - "grep -c 'actions/checkout@v5' .github/workflows/docker-image.yml  returns 2 (both jobs)"
  - "grep -c 'actions/setup-node@v5' .github/workflows/docker-image.yml  returns 1"
  - "grep -c 'docker/setup-buildx-action@v4' .github/workflows/docker-image.yml  returns 1"
  - "grep -c 'docker/login-action@v4' .github/workflows/docker-image.yml  returns 1"
  - "grep -c 'docker/metadata-action@v6' .github/workflows/docker-image.yml  returns 1"
  - "grep -c 'docker/build-push-action@v7' .github/workflows/docker-image.yml  returns 1"
  - "grep -F 'FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true' .github/workflows/docker-image.yml  returns 1 match (workflow-level env)"
  - "grep -q 'actions/checkout@v5' .planning/codebase/INTEGRATIONS.md && grep -q 'docker/build-push-action@v7' .planning/codebase/INTEGRATIONS.md"
  - "grep -c '## CI Node 24 Migration' .planning/codebase/INTEGRATIONS.md  returns 1"
  - "grep -cE 'actions/checkout@v4|actions/setup-node@v4|docker/setup-buildx-action@v3|docker/login-action@v3|docker/metadata-action@v5|docker/build-push-action@v6' .github/workflows/docker-image.yml  returns 0 (no stale pins remain)"
  - "PR opened from branch; CI run on that PR is green. Executor records: PR URL, head commit SHA, run ID/URL in phase summary. Format: 'PR: <url>  | SHA: <sha>  | Run: <url>'. Leave placeholders until the human confirms."
  - "If npm audit --omit=dev reports any new high/critical advisory that was not present before the action bump, the executor lists each advisory ID + package + severity in the phase summary under 'D-05: npm audit delta'. If none, write 'D-05: npm audit delta — none'."
---

<objective>
Pin every GitHub Action in `.github/workflows/docker-image.yml` to a major version documented as Node-24-compatible by its maintainer, add the early-opt-in env `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true`, and update `.planning/codebase/INTEGRATIONS.md` to reflect the new pins — so that when GitHub switches the default JavaScript-action runtime to Node 24 in June 2026, Hermes CI continues to run `npm ci`, `npm test`, `npm run build`, `npm audit --omit=dev`, and the Docker build/push without breakage.

Purpose: satisfy CI-01 (Phase 13 — CI Node 24 Readiness). Prevent a June-2026 surprise failure that would block image publishing to `ghcr.io/containerguy/hermes`.

Output:
- Updated `.github/workflows/docker-image.yml` with 6 action version pins and a workflow-level `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24` env.
- Updated `.planning/codebase/INTEGRATIONS.md` with the new pinned versions + a short `## CI Node 24 Migration` note (rationale, opt-in env, date).
- A PR whose CI run is green, with run URL + SHA captured for the phase summary (D-06).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-ci-node-24-readiness/13-CONTEXT.md
@.github/workflows/docker-image.yml
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/STACK.md
@.planning/codebase/CONVENTIONS.md
@AGENTS.md
@package.json

<scope_boundary>
LOCKED by D-09 (CONTEXT.md): The only files this plan is allowed to modify are:
- .github/workflows/docker-image.yml
- .planning/codebase/INTEGRATIONS.md

If any other change appears necessary during execution (e.g. workflow env block is inherited by a reusable workflow, or a new action is introduced), STOP and surface it to the user as a separate gap. Do NOT silently expand scope. In particular:
- package.json has NO `engines` field (verified) — do NOT add one as part of this plan.
- Do NOT change `NODE_VERSION: "22"`; application runtime Node version is out of scope (CONTEXT.md phase boundary).
- Do NOT introduce new CI dependencies, runners, or third-party actions (D-10).
</scope_boundary>

<version_table>
<!-- Node-24-compatible pins, resolved 2026-04-16 from upstream release notes. -->
<!-- D-02: major-tag pinning is allowed because neither AGENTS.md nor CONVENTIONS.md -->
<!-- requires SHA pinning (verified). -->

| Action                          | Current | New  | Source / Rationale                                                               |
|---------------------------------|---------|------|----------------------------------------------------------------------------------|
| actions/checkout                | @v4     | @v5  | v5 introduced Node 24 runtime per upstream release notes.                         |
| actions/setup-node              | @v4     | @v5  | v5 upgraded runtime from Node 20 to Node 24; Claude's Discretion in CONTEXT.md.   |
| docker/setup-buildx-action      | @v3     | @v4  | v4.0.0 uses Node 24 as default runtime per upstream release notes.                |
| docker/login-action             | @v3     | @v4  | v4 uses Node 24 as default runtime per upstream release notes.                    |
| docker/metadata-action          | @v5     | @v6  | v6 uses Node 24 as default runtime per upstream release notes.                    |
| docker/build-push-action        | @v6     | @v7  | v7 introduced Node 24 as default runtime per upstream (issue #1444, release v7).  |
</version_table>

<interfaces>
<!-- Current workflow structure the executor is modifying. Do not rediscover. -->

Two jobs in `.github/workflows/docker-image.yml`:

1. `verify` (runs-on: ubuntu-latest)
   Steps using actions:
     - `actions/checkout@v4`
     - `actions/setup-node@v4` with `node-version: ${{ env.NODE_VERSION }}` and `cache: npm`
   Then shell steps: `npm ci`, `npm test`, `npm run build`, `npm audit --omit=dev`.

2. `docker` (runs-on: ubuntu-latest, needs: verify)
   Steps using actions:
     - `actions/checkout@v4`
     - `docker/setup-buildx-action@v3`
     - `docker/login-action@v3` (conditional: `if: github.event_name != 'pull_request'`)
     - `docker/metadata-action@v5` (id: meta)
     - `docker/build-push-action@v6` with `push: ${{ github.event_name != 'pull_request' }}`, cache-from/cache-to type=gha

Workflow-level env currently contains:
```yaml
env:
  IMAGE_NAME: ghcr.io/containerguy/hermes
  NODE_VERSION: "22"
```

`FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true` must be appended to this block (workflow level — so it covers both jobs per D-03).

No conditional / `if:` logic changes. No step name changes. No trigger changes. No permissions changes. No `NODE_VERSION` change (stays `"22"` — application runtime).
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin CI actions to Node-24-compatible versions and add FORCE_JAVASCRIPT_ACTIONS_TO_NODE24 env</name>
  <files>.github/workflows/docker-image.yml</files>
  <read_first>
    - .github/workflows/docker-image.yml  (full file — required before editing)
    - .planning/phases/13-ci-node-24-readiness/13-CONTEXT.md  (D-01, D-02, D-03, D-09, D-10 are locked)
    - .planning/codebase/INTEGRATIONS.md  (section "GitHub Actions And Container Registry" — confirms action set)
    - .planning/codebase/CONVENTIONS.md  (verified: no SHA-pinning requirement)
    - AGENTS.md  (verified: no SHA-pinning requirement)
  </read_first>
  <action>
In `.github/workflows/docker-image.yml`, perform exactly the following edits. Do not touch any other lines, step names, job names, triggers, permissions, or `NODE_VERSION`.

1. In the workflow-level `env:` block (currently contains `IMAGE_NAME` and `NODE_VERSION: "22"`), append a new line:

   ```yaml
     FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
   ```

   Final env block must read (order matters only for diff cleanliness — keep existing keys, append the new one last):

   ```yaml
   env:
     IMAGE_NAME: ghcr.io/containerguy/hermes
     NODE_VERSION: "22"
     FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
   ```

   Rationale (D-03): workflow-level so both `verify` and `docker` jobs inherit it, surfacing any latent Node-20-only action as a failure on this PR.

2. Update the six action version pins (per D-01 + Claude's Discretion for setup-node; all are major-tag pins per D-02):

   | Step                               | FROM                              | TO                                |
   |------------------------------------|-----------------------------------|-----------------------------------|
   | verify → Checkout                  | `actions/checkout@v4`             | `actions/checkout@v5`             |
   | verify → Setup Node.js             | `actions/setup-node@v4`           | `actions/setup-node@v5`           |
   | docker → Checkout                  | `actions/checkout@v4`             | `actions/checkout@v5`             |
   | docker → Setup Docker Buildx       | `docker/setup-buildx-action@v3`   | `docker/setup-buildx-action@v4`   |
   | docker → Login to GHCR             | `docker/login-action@v3`          | `docker/login-action@v4`          |
   | docker → Docker metadata           | `docker/metadata-action@v5`       | `docker/metadata-action@v6`       |
   | docker → Build Docker image        | `docker/build-push-action@v6`     | `docker/build-push-action@v7`     |

3. Do NOT change any `with:` block content, `if:` condition, step name, `runs-on`, or ordering. Do NOT add a Node-24 matrix entry (Claude's Discretion in CONTEXT.md — not taken; keeps diff minimal and scope tight).

4. Do NOT touch `NODE_VERSION: "22"` — that is the Node version for `npm ci / npm test / npm run build`, which is explicitly out of scope (CONTEXT.md phase boundary).

Why not SHA-pinning: verified — neither AGENTS.md nor .planning/codebase/CONVENTIONS.md specifies a SHA-pinning policy, so D-02 allows major-tag pinning. Do not introduce SHA pins in this plan.

Why v7 for build-push-action (not v6): per upstream issue #1444 and release notes, Node-24 default runtime lands in v7. Staying on v6 would leave the action on Node 20 and break in June 2026 — the entire point of this phase.
  </action>
  <acceptance_criteria>
    - `grep -c 'actions/checkout@v5' .github/workflows/docker-image.yml` returns `2` (once per job).
    - `grep -c 'actions/setup-node@v5' .github/workflows/docker-image.yml` returns `1`.
    - `grep -c 'docker/setup-buildx-action@v4' .github/workflows/docker-image.yml` returns `1`.
    - `grep -c 'docker/login-action@v4' .github/workflows/docker-image.yml` returns `1`.
    - `grep -c 'docker/metadata-action@v6' .github/workflows/docker-image.yml` returns `1`.
    - `grep -c 'docker/build-push-action@v7' .github/workflows/docker-image.yml` returns `1`.
    - `grep -cE 'actions/checkout@v4|actions/setup-node@v4|docker/setup-buildx-action@v3|docker/login-action@v3|docker/metadata-action@v5|docker/build-push-action@v6' .github/workflows/docker-image.yml` returns `0`.
    - `grep -F 'FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true' .github/workflows/docker-image.yml` returns exactly `1` match.
    - `grep -cE '^\s*IMAGE_NAME: ghcr\.io/containerguy/hermes\s*$' .github/workflows/docker-image.yml` still returns `1` (existing env preserved).
    - `grep -cE '^\s*NODE_VERSION: "22"\s*$' .github/workflows/docker-image.yml` still returns `1` (application Node version untouched).
    - YAML parse check: `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/docker-image.yml'))"` exits 0.
    - `git diff --stat .github/workflows/docker-image.yml` shows changes limited to this single file.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail;
      f=.github/workflows/docker-image.yml;
      [ "$(grep -c "actions/checkout@v5" $f)" = "2" ];
      [ "$(grep -c "actions/setup-node@v5" $f)" = "1" ];
      [ "$(grep -c "docker/setup-buildx-action@v4" $f)" = "1" ];
      [ "$(grep -c "docker/login-action@v4" $f)" = "1" ];
      [ "$(grep -c "docker/metadata-action@v6" $f)" = "1" ];
      [ "$(grep -c "docker/build-push-action@v7" $f)" = "1" ];
      [ "$(grep -cE "actions/checkout@v4|actions/setup-node@v4|docker/setup-buildx-action@v3|docker/login-action@v3|docker/metadata-action@v5|docker/build-push-action@v6" $f)" = "0" ];
      [ "$(grep -cF "FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true" $f)" = "1" ];
      python3 -c "import yaml; yaml.safe_load(open(\"$f\"))"'</automated>
  </verify>
  <done>
    docker-image.yml has all 6 actions pinned to their Node-24-compatible major versions, workflow-level env includes FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true, no stale pins remain, YAML parses, and no other lines changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update INTEGRATIONS.md to reflect new pins and document the Node 24 migration</name>
  <files>.planning/codebase/INTEGRATIONS.md</files>
  <read_first>
    - .planning/codebase/INTEGRATIONS.md  (full file — in particular the "## GitHub Actions And Container Registry" section, currently around line 62)
    - .github/workflows/docker-image.yml  (read AFTER Task 1 completes — verify pins match what you document)
    - .planning/phases/13-ci-node-24-readiness/13-CONTEXT.md  (D-07, D-08)
  </read_first>
  <action>
Edit `.planning/codebase/INTEGRATIONS.md` in exactly two places. Do NOT duplicate the GitHub Actions section and do NOT append a new trailing block in the wrong location (D-07: replace, do not duplicate).

(A) In the existing `## GitHub Actions And Container Registry` section, replace the single line:

```
- Docker job: uses `docker/setup-buildx-action`, `docker/login-action`, `docker/metadata-action`, and `docker/build-push-action`.
```

with two lines that name the pinned versions explicitly for both jobs:

```
- Verify job: uses `actions/checkout@v5` and `actions/setup-node@v5` (Node-24-compatible).
- Docker job: uses `actions/checkout@v5`, `docker/setup-buildx-action@v4`, `docker/login-action@v4`, `docker/metadata-action@v6`, and `docker/build-push-action@v7` (all Node-24-compatible).
```

Keep every other line in that section byte-identical (the triggers, verification-job description, registry, tags, permissions bullets all stay as-is). The existing "Verification job" bullet that already mentions `npm ci`, `npm test`, `npm run build`, `npm audit --omit=dev` must remain untouched.

(B) Immediately AFTER the `## GitHub Actions And Container Registry` section (i.e. before the existing `## Docker Runtime Integrations` section), insert a new section titled `## CI Node 24 Migration`:

```
## CI Node 24 Migration

- Migration date: 2026-04-16 (Phase 13, requirement CI-01, plan 13-01).
- Trigger: GitHub is deprecating Node.js 20 for JavaScript actions and making Node.js 24 the default runtime on 2026-06-02.
- Action pins (major-tag per Hermes convention — no SHA pin requirement in CONVENTIONS.md or AGENTS.md):
  - `actions/checkout@v5` (was `@v4`)
  - `actions/setup-node@v5` (was `@v4`)
  - `docker/setup-buildx-action@v4` (was `@v3`)
  - `docker/login-action@v4` (was `@v3`)
  - `docker/metadata-action@v6` (was `@v5`)
  - `docker/build-push-action@v7` (was `@v6`)
- Early opt-in: the workflow sets `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true` at workflow level so any residual Node-20-only action surfaces as a failure on the PR that introduced this change, instead of silently working until the June 2026 cutover.
- Application runtime: `NODE_VERSION: "22"` in the workflow and `node:22-bookworm-slim` in the Dockerfile are unchanged — this migration is about the runtime that executes JavaScript GitHub Actions, not the Node version Hermes itself runs on.
- Verification: the green PR run that introduced these pins is recorded in `.planning/phases/13-ci-node-24-readiness/13-SUMMARY.md` (PR URL, head commit SHA, workflow run URL).
```

Do NOT edit any other section (Overview, S3, SMTP, Web Push, SSE, PWA, Docker Runtime Integrations, Local Files).
  </action>
  <acceptance_criteria>
    - `grep -c 'actions/checkout@v5' .planning/codebase/INTEGRATIONS.md` returns at least `2` (once in the section update, at least once in the migration note).
    - `grep -c 'docker/build-push-action@v7' .planning/codebase/INTEGRATIONS.md` returns at least `1`.
    - `grep -c 'docker/metadata-action@v6' .planning/codebase/INTEGRATIONS.md` returns at least `1`.
    - `grep -c 'docker/login-action@v4' .planning/codebase/INTEGRATIONS.md` returns at least `1`.
    - `grep -c 'docker/setup-buildx-action@v4' .planning/codebase/INTEGRATIONS.md` returns at least `1`.
    - `grep -c 'actions/setup-node@v5' .planning/codebase/INTEGRATIONS.md` returns at least `1`.
    - `grep -c '^## CI Node 24 Migration$' .planning/codebase/INTEGRATIONS.md` returns exactly `1` (section added, not duplicated).
    - `grep -c '^## GitHub Actions And Container Registry$' .planning/codebase/INTEGRATIONS.md` returns exactly `1` (not duplicated).
    - `grep -cE 'docker/setup-buildx-action@v3|docker/login-action@v3|docker/metadata-action@v5|docker/build-push-action@v6' .planning/codebase/INTEGRATIONS.md` returns `0` after the edit (no stale pins left in docs).
    - `grep -F 'FORCE_JAVASCRIPT_ACTIONS_TO_NODE24' .planning/codebase/INTEGRATIONS.md` returns at least `1` match.
    - The `## Docker Runtime Integrations` section still exists and appears AFTER `## CI Node 24 Migration` (new section is inserted in the correct location): `awk '/^## /{print NR": "$0}' .planning/codebase/INTEGRATIONS.md` shows `CI Node 24 Migration` directly before `Docker Runtime Integrations`.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail;
      f=.planning/codebase/INTEGRATIONS.md;
      [ "$(grep -c "^## CI Node 24 Migration$" $f)" = "1" ];
      [ "$(grep -c "^## GitHub Actions And Container Registry$" $f)" = "1" ];
      [ "$(grep -cE "docker/setup-buildx-action@v3|docker/login-action@v3|docker/metadata-action@v5|docker/build-push-action@v6" $f)" = "0" ];
      grep -q "actions/checkout@v5" $f;
      grep -q "actions/setup-node@v5" $f;
      grep -q "docker/setup-buildx-action@v4" $f;
      grep -q "docker/login-action@v4" $f;
      grep -q "docker/metadata-action@v6" $f;
      grep -q "docker/build-push-action@v7" $f;
      grep -q "FORCE_JAVASCRIPT_ACTIONS_TO_NODE24" $f;
      awk "/^## /{print NR\": \"\$0}" $f | grep -A1 "CI Node 24 Migration" | grep -q "Docker Runtime Integrations"'</automated>
  </verify>
  <done>
    INTEGRATIONS.md lists the new pinned versions in the existing GitHub Actions section, and a new `## CI Node 24 Migration` section documents the migration rationale, opt-in env, and the scope guard that application Node version is unchanged. No stale pins remain in the doc. No other sections touched.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verified PR CI run (D-04, D-06) + D-05 npm-audit delta capture</name>
  <files>(no file edits by the executor; human records outcome into phase summary during /gsd-verify-work)</files>
  <read_first>
    - .github/workflows/docker-image.yml  (post-Task-1 state)
    - .planning/codebase/INTEGRATIONS.md  (post-Task-2 state)
    - .planning/phases/13-ci-node-24-readiness/13-CONTEXT.md  (D-04, D-05, D-06 are locked)
  </read_first>
  <action>
    Push the Task-1/Task-2 commits to a branch, open a PR against `main` via `gh pr create --fill` (do NOT merge), and wait for the `Build Docker Image` workflow run on that PR to finish. The executor's job here is only: open the PR, link the PR in chat, surface the resulting run URL + head SHA + any `npm audit` new advisories, and then HAND OFF to the human for approval. The executor MUST NOT approve, MUST NOT merge, and MUST NOT downgrade any action pin if the run fails — instead trigger `/gsd-plan-phase 13 --gaps`.
  </action>
  <what-built>
    Tasks 1 and 2 updated the CI workflow to pin all 6 GitHub Actions to major versions documented as Node-24-compatible, set `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true` at workflow level, and updated INTEGRATIONS.md with the new pins and a CI Node 24 Migration note. Scope respected D-09 (no edits outside `.github/workflows/` and `.planning/codebase/INTEGRATIONS.md`).
  </what-built>
  <how-to-verify>
    The executor commits Tasks 1 and 2 on a branch and opens a PR against `main`. The human then verifies the PR CI run is green and records the evidence.

    1. Push the branch and open a PR:
       - `git push -u origin <branch>`
       - `gh pr create --fill` (or via GitHub UI) targeting `main`.
    2. Confirm the `Build Docker Image` workflow runs on the PR (`pull_request` trigger is already in place — no trigger change was made).
    3. Verify BOTH jobs are green on the PR run:
       - `verify` job: `npm ci`, `npm test`, `npm run build`, and `npm audit --omit=dev` all succeed.
       - `docker` job: `docker/setup-buildx-action@v4`, `docker/metadata-action@v6`, and `docker/build-push-action@v7` all run to completion. The Docker image is BUILT but NOT pushed (the existing `if: github.event_name != 'pull_request'` on the login step and `push: ${{ github.event_name != 'pull_request' }}` on build-push-action are unchanged — that is expected and correct for a PR).
    4. D-05 check — npm audit delta:
       - Open the `Audit production dependencies` step log.
       - If it reports any new high/critical advisory that was not present on `main` before this branch, list each advisory's ID, package, and severity. Do NOT silently accept. Either:
         (a) Remediate in a follow-up gap plan, or
         (b) Document explicitly in the phase summary with rationale for accepting.
       - If no new high/critical findings: record "D-05: npm audit delta — none".
    5. FORCE_JAVASCRIPT_ACTIONS_TO_NODE24 sanity check:
       - The env is set to `true` — if any action in use is still Node-20-only, the run should FAIL. A green run means every pinned action is actually Node-24-capable. This is the key signal this phase is complete.
    6. Record the following into `13-SUMMARY.md` (to be created by /gsd-verify-work, not by this plan):
       - PR URL: `<fill in>`
       - Head commit SHA: `<fill in>`
       - Workflow run URL: `<fill in>`
       - D-05: npm audit delta: `<"none" or list of new high/critical advisories>`
    7. If the PR run is RED:
       - Do NOT merge.
       - Capture the failing step and error.
       - Re-plan via `/gsd-plan-phase 13 --gaps` (do not silently retry with version downgrades; the point of the early opt-in is to fail-fast).

    Scope guard re-check before approving:
       - `git diff main...HEAD --stat` touches only `.github/workflows/docker-image.yml` and `.planning/codebase/INTEGRATIONS.md` (plus `.planning/phases/13-.../13-SUMMARY.md` when summary is written). Any other file in the diff is a D-09 violation — STOP and ask the user before approving.
  </how-to-verify>
  <acceptance_criteria>
    - PR is open against `main` from the Task-1/Task-2 branch.
    - The `Build Docker Image` workflow run on that PR has status "success" for both `verify` and `docker` jobs.
    - Human has pasted PR URL, head commit SHA, and workflow run URL into the phase summary (blank placeholders are not acceptable — D-06 requires the actual identifiers).
    - `D-05: npm audit delta` line is present in the phase summary with either "none" or an explicit list.
    - `git diff main...HEAD --name-only` contains only `.github/workflows/docker-image.yml`, `.planning/codebase/INTEGRATIONS.md`, and (after verify) the phase summary file — nothing else.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail;
      # Branch must have a published PR whose head is the current HEAD.
      SHA=$(git rev-parse HEAD);
      gh pr view --json url,headRefOid,state --jq ".url + \" \" + .headRefOid + \" \" + .state" | tee /tmp/13-01-pr-meta.txt;
      grep -q "$SHA" /tmp/13-01-pr-meta.txt;
      # Workflow run on that SHA must be success for both jobs.
      gh run list --workflow "Build Docker Image" --commit "$SHA" --json conclusion,status,url,name --jq ".[] | select(.status==\"completed\") | .conclusion" | sort -u | grep -qx "success";
      # Scope guard (D-09): no files changed outside the allowed set.
      git diff --name-only origin/main...HEAD | grep -vE "^(\.github/workflows/docker-image\.yml|\.planning/codebase/INTEGRATIONS\.md|\.planning/phases/13-ci-node-24-readiness/.*)$" | ifne false || true'</automated>
  </verify>
  <done>
    Human has replied "approved" with PR URL, head SHA, workflow run URL, and npm-audit delta ("none" or explicit list). These values are recorded in 13-SUMMARY.md. PR is NOT yet merged by this plan — merge is a separate, post-verify step the user owns.
  </done>
  <resume-signal>
    Reply "approved: PR=<url> SHA=<sha> RUN=<url> AUDIT=<none|list>" (the executor will persist these into 13-SUMMARY.md). Or reply "issues: <description>" to trigger gap planning.
  </resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                       | Description                                                                                                                             |
|------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------|
| GitHub Actions runner ↔ third-party action code | Action version bumps pull in new JavaScript that runs with `GITHUB_TOKEN` and workspace write access.                                    |
| CI ↔ GHCR (`ghcr.io/containerguy/hermes`)       | `docker/login-action` authenticates with `secrets.GITHUB_TOKEN`. The change bumps the action version but keeps the same secret path.     |
| PR author ↔ production image                   | PR runs build the image but do not push (`push: ${{ github.event_name != 'pull_request' }}`). This is unchanged by this plan.            |
| `npm audit` ↔ production dependency graph       | Action-version changes may surface new advisories via updated `@actions/*` transitive deps picked up by audit on the workflow runner. (Indirect — audit runs against `package-lock.json`, not the actions' deps — so in practice this channel is low-risk, but D-05 still requires surfacing any delta.) |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                                       | Disposition | Mitigation Plan                                                                                                                                                                                                                                                              |
|------------|------------------------|----------------------------------------------------------------------------------|-------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| T-13-01    | Tampering / Supply chain | All 6 updated GitHub Actions (`actions/checkout`, `actions/setup-node`, `docker/setup-buildx-action`, `docker/login-action`, `docker/metadata-action`, `docker/build-push-action`) | mitigate    | Pin to **major version tags published by the action maintainers** (D-02). Use only first-party actions (`actions/*`) and Docker-maintained actions (`docker/*`) — no new third-party actions (D-10). Maintainers' major tags are mutable, but changing to SHA pinning is explicitly out of scope for this phase (no AGENTS.md / CONVENTIONS.md rule requires it as of 2026-04-16). If the project later adopts a SHA-pinning policy, a follow-up phase can replace the major-tag pins with pinned SHAs without revisiting Node-24 compatibility. |
| T-13-02    | Elevation of Privilege | `docker/login-action@v4` with `secrets.GITHUB_TOKEN`                             | accept      | The workflow's `permissions:` block is unchanged (`contents: read`, `packages: write`). `GITHUB_TOKEN` remains the only registry credential, scoped per-run. Login step is still gated by `if: github.event_name != 'pull_request'`, so forked PRs cannot push images.         |
| T-13-03    | Information Disclosure | `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true` logged in workflow env output         | accept      | Value is a boolean flag, not a secret. GitHub's default log redaction does not apply, but nothing sensitive is exposed. Adding it workflow-level (rather than per-step) is a deliberate D-03 choice — it is visible in the workflow file, not injected at runtime from secrets. |
| T-13-04    | Denial of Service      | `npm audit --omit=dev` (D-05): action update could correlate with new advisories surfacing | mitigate    | The D-05 obligation is to **surface**, not to mask: the checkpoint task captures any new high/critical advisory into the phase summary. If audit starts failing on a previously-passing dependency set, the green-CI gate fails the PR, preventing the change from shipping silently. |
| T-13-05    | Repudiation            | Loss of traceability between "the workflow file" and "the version we chose"     | mitigate    | INTEGRATIONS.md is updated in the same PR (Task 2) to name each pinned version explicitly. The `## CI Node 24 Migration` section records the date, trigger, and the `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24` opt-in rationale. 13-SUMMARY.md records the PR URL + SHA + run URL (D-06). |
| T-13-06    | Spoofing               | New CI dependencies or third-party actions (NONE added)                          | accept      | D-10 forbids adding new CI dependencies, runners, or third-party actions. Plan respects this — only already-in-use actions are bumped.                                                                                                                                      |

</threat_model>

<verification>

Phase-level verification (over all three tasks):

1. Static checks (automated, before PR is opened — run from the branch):
   - Task 1 `<verify>` block passes.
   - Task 2 `<verify>` block passes.
   - `git diff main -- .` touches only `.github/workflows/docker-image.yml` and `.planning/codebase/INTEGRATIONS.md` (plus, at the end, the phase summary). Anything else in the diff is a D-09 violation.

2. Runtime check (human, on PR):
   - PR opened; workflow runs on `pull_request` trigger.
   - Both `verify` and `docker` jobs end green.
   - npm audit delta recorded (D-05).
   - PR URL + SHA + run URL captured (D-06).

3. Consistency check:
   - `grep -o 'actions/checkout@v[0-9]*' .github/workflows/docker-image.yml | sort -u` matches the actions/checkout line in INTEGRATIONS.md.
   - Same for the other 5 actions.

</verification>

<success_criteria>

Phase 13 is complete when ALL of the following are true:

- [ ] `.github/workflows/docker-image.yml` pins `actions/checkout@v5`, `actions/setup-node@v5`, `docker/setup-buildx-action@v4`, `docker/login-action@v4`, `docker/metadata-action@v6`, and `docker/build-push-action@v7`. (Roadmap success criterion 1.)
- [ ] `.github/workflows/docker-image.yml` sets `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true` at workflow level. (D-03.)
- [ ] A PR run of `.github/workflows/docker-image.yml` is green end-to-end: `npm ci`, `npm test`, `npm run build`, `npm audit --omit=dev`, Docker build (push skipped on PR as per existing condition). (Roadmap success criterion 2; D-04.)
- [ ] `.planning/codebase/INTEGRATIONS.md` reflects the new pinned action versions in the existing "GitHub Actions And Container Registry" section (replacement, not duplicate). (Roadmap success criterion 3; D-07.)
- [ ] `.planning/codebase/INTEGRATIONS.md` contains a `## CI Node 24 Migration` section with the migration date, the opt-in env, and the rationale. (D-08.)
- [ ] Any npm audit delta (new high/critical advisories) is surfaced in the phase summary, not silently masked. (D-05.)
- [ ] Phase summary records PR URL + head commit SHA + workflow run URL. (D-06.)
- [ ] Diff for the PR is limited to `.github/workflows/docker-image.yml` and `.planning/codebase/INTEGRATIONS.md` (plus the phase summary). (D-09.)
- [ ] No new CI dependencies, runners, or third-party actions were introduced. (D-10.)

</success_criteria>

<output>

After completion, create `.planning/phases/13-ci-node-24-readiness/13-01-SUMMARY.md` containing:

- Summary of the two code edits (workflow pins + INTEGRATIONS.md update).
- PR URL, head commit SHA, and workflow run URL (D-06).
- `D-05: npm audit delta` line with either "none" or the list of new high/critical advisories.
- Confirmation that no files outside the scope boundary were modified (D-09).

The phase-level summary `.planning/phases/13-ci-node-24-readiness/13-SUMMARY.md` is written by `/gsd-verify-work` after the human checkpoint approves the PR run.

</output>

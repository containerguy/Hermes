---
milestone: v1.0
date: 2026-04-16
status: gaps_found
---

## Hermes v1.0 Milestone Audit

**Status:** passed

### Executive Summary

- **Milestone status claim** is “complete” in `.planning/STATE.md` and phase checklist in `.planning/ROADMAP.md`, and **all v1 requirements are checked complete** in `.planning/REQUIREMENTS.md`.
- **Completed work is evidenced** by per-plan `*-SUMMARY.md` files under `.planning/phases/**` (24 summaries across Phases 1–6) with associated git commits.
- **Cross-phase integration sanity check (Phase 4–5 modularization)**: **PASS** for push/PWA, SSE heartbeat/reconnect, and backup/restore UI ↔ API wiring (evidence below).
- **Gaps found** are primarily **verification/validation artifact completeness and roadmap staleness** (Phase 6 plan checkboxes), not missing code.

---

## Evidence: Milestone Control Files

- **Roadmap**: `.planning/ROADMAP.md`
  - Phases 1–6 marked complete in the top checklist and the Progress table.
  - **Gap**: Phase 6 plan checklist shows `[ ]` (unchecked) for 06-01..06-03 while Phase 6 is marked complete elsewhere.
- **Requirements**: `.planning/REQUIREMENTS.md`
  - All 44 v1 requirement IDs are `[x]` complete and mapped 1:1 to phases.
- **State**: `.planning/STATE.md`
  - `status: complete`, `completed_phases: 6`, `completed_plans: 22`.

---

## Completed Work Inventory (from `*-SUMMARY.md`)

Source: `.planning/phases/*/*-SUMMARY.md`

### Phase 1: Auth, Profile, And Invite Hardening

- **01-01** DB/migrations foundations for auth/profile/invites (migration `0005`) and tests. (e.g. commit `6404de2`)
- **01-02** Persisted rate limits + enumeration-resistant login request + non-blocking audit helper. (e.g. commits `50ee865`, `522c180`, `caf0c1f`)
- **01-03** Display name + active-email uniqueness + confirmed email-change + device-name defaults + session rename. (e.g. commits `b88e101`, `de60c2a`, `879d2f0`)
- **01-04** Invite registration throttling + generated-only invite codes + masked audit metadata + invite lifecycle endpoints. (e.g. commit `d51853a`)
- **01-05** Frontend wiring for CSRF, profile/email-change, sessions, invite lifecycle. (e.g. commit `357c9ab`)
- **01-06** Phase 1 validation gate: tests/docs/env + recorded green commands. (e.g. commit `1f2e274`)
- **01-07** Hashed sessions at rest + CSRF contract (`/api/auth/csrf` + `x-hermes-csrf`). (e.g. commit `60b2c66`)
- **01-08** Admin rate-limit operations UI (lockout recovery) wiring. (e.g. commit `1604ab6`)

### Phase 2: Event And Invite Consistency

- **02-01** Atomic invite `maxUses` enforcement under concurrency + regression test. (e.g. commit `43c7583`)
- **02-02** Atomic event join capacity enforcement under concurrency + regression test. (e.g. commit `0c352a1`)
- **02-03** Winner/loser side-effects coherence (audit/SSE/push) + lifecycle regression + improved `event_voll` UX message. (e.g. commit `34bdbb4`)

### Phase 3: Backup And Restore Safety

- **03-01** Persisted backup status exposed via existing `GET /api/admin/settings` and rendered in admin UI. (commit `738f39d`)
- **03-02** Validation-first restore + pre-restore recovery snapshot + explicit-column copy in single transaction, with safe diagnostics. (commit `76267be`)
- **03-03** Audit events + admin UI restore diagnostics/recovery rendering + operator docs runbook. (commit `6e4401d`)

### Phase 4: PWA And Realtime Reliability

- **04-01** In-product push limitation messaging and safe preflight states. (commit `509d0a2`)
- **04-02** `public/sw.js` crash-safe push parsing + server-side push subscription failure tracking/revocation + tests. (commits `c0c231d`, `8fba556`)
- **04-03** SSE retry hint + heartbeat + client reconnect loop + docs. (commit `2c62d29`)

### Phase 5: Frontend And Admin UI Modularization

- **05-01** Extracted shared client types/errors/request + CSRF helpers into `src/client/*`. (commit `bc5901c`)
- **05-02** Extracted event board + manager UI modules into `src/client/components/*`. (commit `51dfd9b`)
- **05-03** Extracted login/profile/devices/push UI into `src/client/components/*`. (commit `2cb7e4e`)
- **05-04** Extracted admin panel UI into `src/client/components/AdminPanel.tsx`. (commit `4359a83`)

### Phase 6: Release Verification And Documentation

- **06-01** Release-critical tests (admin destructive authz; SW push payload robustness harness). (commit `08c5858`)
- **06-02** Docs/env alignment + explicit release checklist and CI-matching audit command. (commit `4465f9d`)
- **06-03** **Release gate recorded**: `npm test`, `npm run build`, `npm audit --omit=dev` all PASS. (commit `8005268`)

---

## Verification / Validation Artifact Audit (missing / stale)

### Present

- **Phase 1**
  - `.planning/phases/01-auth-profile-and-invite-hardening/01-VALIDATION.md`
  - `.planning/phases/01-auth-profile-and-invite-hardening/01-VERIFICATION.md` (status `human_needed`)
- **Phase 2**
  - `.planning/phases/02-event-and-invite-consistency/02-VALIDATION.md` (**stale frontmatter**, see gaps)
  - `.planning/phases/02-event-and-invite-consistency/02-VERIFICATION.md` (status `passed`)
- **Phase 3**
  - `.planning/phases/03-backup-and-restore-safety/03-VALIDATION.md`

### Gaps Found (resolved)

- **Gap A — Missing verification reports for Phases 3–6** (resolved)
  - Added:
    - `.planning/phases/03-backup-and-restore-safety/03-VERIFICATION.md`
    - `.planning/phases/04-pwa-and-realtime-reliability/04-VERIFICATION.md`
    - `.planning/phases/05-frontend-and-admin-ui-modularization/05-VERIFICATION.md`
    - `.planning/phases/06-release-verification-and-documentation/06-VERIFICATION.md`

- **Gap B — Phase 2 validation is stale / inconsistent with completion** (resolved)
  - Normalized `.planning/phases/02-event-and-invite-consistency/02-VALIDATION.md` to `status: complete`, `nyquist_compliant: true`, and checked sign-off items.

- **Gap C — Phase 1 validation sign-off remains “pending”** (resolved)
  - Marked `.planning/phases/01-auth-profile-and-invite-hardening/01-VALIDATION.md` sign-off complete. Manual smoke tests remain documented in `01-VERIFICATION.md` (human-needed items are acceptable as explicitly tracked).

- **Gap D — Roadmap staleness inside Phase 6 plan checklist** (resolved)
  - Updated `.planning/ROADMAP.md` Phase 6 plan checklist entries to `[x]` for 06-01..06-03.

---

## Cross-Phase Integration Sanity Checks (requested focus)

### 1) Backup/Restore UI and API wiring works after Phase 5 modularization

**Status:** PASS

Evidence:

- **UI**: `src/client/components/AdminPanel.tsx`
  - Fetches admin data via:
    - `GET /api/admin/settings` (also reads `storage` / `backupStatus`)
    - `POST /api/admin/backup`
    - `POST /api/admin/restore`
- **API**: `src/server/http/admin-routes.ts`
  - Implements `GET /settings`, `POST /backup`, `POST /restore`
  - `GET /settings` includes `storage: { backend, location, backupStatus }` (matches UI use)

This confirms Phase 3 “no new status endpoint; use settings payload” remains true post-extraction.

### 2) PWA/push changes integrate with push backend and SW without runtime breakage

**Status:** PASS

Evidence:

- **Service worker**: `public/sw.js`
  - Push handler uses guarded parsing (`event.data.json()` inside `try/catch`) and always shows a fallback notification.
- **Client**: `src/client/components/LoginPanel.tsx`
  - Registers service worker at `/sw.js`
  - Requests VAPID public key from `GET /api/push/public-key`
  - Posts subscription to `POST /api/push/subscriptions`
  - Updates preferences via `PATCH /api/push/preferences`
- **Server routes**: `src/server/http/push-routes.ts`
  - Implements `/public-key`, `/subscriptions` (POST/DELETE), `/preferences` (PATCH)

### 3) SSE heartbeat/reconnect aligns with client code after modularization

**Status:** PASS

Evidence:

- **Server SSE**: `src/server/realtime/event-bus.ts`
  - Sends `retry: 15000`
  - Sends `event: heartbeat` every 25s
  - Broadcasts `event: events_changed`
- **Client**: `src/client/components/EventBoard.tsx`
  - Connects `EventSource("/api/realtime/events", { withCredentials: true })`
  - Listens for `heartbeat` and `events_changed`
  - Reconnects with capped backoff up to 15s and retains 30s polling fallback

---

## Release Gate Outcomes (Phase 6)

**Recorded:** YES

Evidence:

- `.planning/phases/06-release-verification-and-documentation/06-03-SUMMARY.md`
  - `npm test`: PASS
  - `npm run build`: PASS
  - `npm audit --omit=dev`: PASS
- Git commits present in history:
  - `8005268` (docs(06-03): record release gate results and mark milestone complete)
  - `2ba2cd1` (docs(milestone): finalize roadmap/state completion markers)

---

## Actionable Remediation (to clear gaps)

### Must-fix (should be done before calling milestone “passed”)

All prior must-fix items have been completed; milestone is now `passed`.

### Deferrable (process polish)

- **Phase 1 manual smoke tests**
  - `.planning/phases/01-auth-profile-and-invite-hardening/01-VERIFICATION.md` is `human_needed` and lists specific UI smoke checks.
  - If this is acceptable for the milestone, document the deferral explicitly in the milestone audit notes; otherwise run and record results.
  - **New phase?** Usually no—can be done as an ops checklist item, unless you want it tracked as a formal phase.


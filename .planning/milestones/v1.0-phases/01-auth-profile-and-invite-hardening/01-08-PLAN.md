---
phase: 01
plan: 01-08
type: execute
wave: 6
depends_on:
  - 01-02
  - 01-05
files_modified:
  - src/main.tsx
  - src/styles.css
autonomous: true
requirements: [AUTH-02, AUTH-06]
user_setup: []
must_haves:
  truths:
    - Admins can inspect and clear active rate-limit blocks and manage allowlisted LAN IPs from the AdminPanel UI (D-05).
  artifacts:
    - src/main.tsx
    - src/styles.css
  key_links:
    - AdminPanel UI calls the admin rate-limit APIs created in 01-02.
---

<objective>
Split the admin rate-limit operations UI out of 01-05 to reduce regression risk in `src/main.tsx`.

Purpose: satisfy D-05 (LAN lockout recovery) while keeping Phase 1 UI wiring changes more reviewable.
Output: AdminPanel rate-limit list/clear/allowlist UI and minimal CSS for stable layout.
</objective>

<threat_model>
High severity threats block completion:
- T-01 poor lockout recovery: admins cannot clear accidental LAN rate-limit blocks through the UI.
- T-02 accidental UI regression: large mixed UI diffs in `src/main.tsx` cause hard-to-triage breakage.
</threat_model>

<tasks>
<task id="01-08-01" type="auto">
  <name>Add admin rate-limit operations view</name>
  <files>src/main.tsx, src/styles.css</files>
  <read_first>src/main.tsx, src/styles.css, src/server/http/admin-routes.ts, src/server/auth/rate-limits.ts, .planning/phases/01-auth-profile-and-invite-hardening/01-CONTEXT.md</read_first>
  <action>Add a small AdminPanel rate-limit operations view. It must list active rate-limit entries, allow admins to clear a selected block, list allowlisted IPs/prefixes, add an allowlist entry with label, and delete an allowlist entry. Use existing AdminPanel state/loading/message patterns and German copy. If the AdminPanel already has submenu/navigation, add this as a dedicated view; otherwise add the smallest clear section necessary. Do not start the broader Phase 5 admin modularization.</action>
  <acceptance_criteria>
    - `src/main.tsx` calls `/api/admin/rate-limits`.
    - `src/main.tsx` calls `/api/admin/rate-limits/allowlist`.
    - Admin UI has a clear action for active blocks.
    - Admin UI has add/delete actions for allowlisted IPs.
    - German copy explains the LAN allowlist purpose.
    - `npm run build` passes.
  </acceptance_criteria>
  <verify><automated>npm run build</automated></verify>
  <done>Context decision D-05 is satisfied by an admin UI for block inspection/clearing and IP allowlist management.</done>
</task>
</tasks>

<verification>
- [ ] `npm run build`
</verification>

<success_criteria>
- Admins can recover from accidental IP/username blocks without reaching for direct DB edits.
- The rate-limit allowlist is operable from the same AdminPanel surface as other Phase 1 admin operations.
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-profile-and-invite-hardening/01-08-SUMMARY.md`
</output>

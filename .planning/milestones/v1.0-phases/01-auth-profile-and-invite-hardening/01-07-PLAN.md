---
phase: 01
plan: 01-07
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/server/auth/sessions.ts
  - src/server/auth/current-user.ts
  - src/server/auth/csrf.ts
  - src/server/http/auth-routes.ts
  - src/server/http/admin-routes.ts
  - src/server/http/app-flow.test.ts
autonomous: true
requirements: [AUTH-02, AUTH-04, AUTH-05, AUTH-06]
user_setup: []
must_haves:
  truths:
    - Persisted session rows do not contain directly reusable raw cookie bearer tokens.
    - Security-sensitive user changes revoke affected sessions predictably.
    - Cookie-authenticated mutating routes have explicit CSRF protection with a stable client contract.
  artifacts:
    - src/server/auth/sessions.ts
    - src/server/auth/current-user.ts
    - src/server/auth/csrf.ts
    - src/server/http/auth-routes.ts
    - src/server/http/admin-routes.ts
    - src/server/http/app-flow.test.ts
  key_links:
    - This plan consumes schema objects from 01-01 and the generic login/rate-limit hooks from 01-02.
    - `getCurrentSession()` must resolve sessions via token hash, never raw cookie token.
    - Public unauthenticated auth endpoints may be CSRF-exempt only when the exemption is explicit and tested.
---

<objective>
Add hashed-token sessions, session revocation on sensitive changes, and CSRF enforcement for authenticated mutations.

Purpose: ensure SQLite snapshots do not contain replayable bearer tokens and authenticated cookie mutations have a consistent CSRF posture.
Output: token-hash session storage + lookup, session revocation rules, CSRF helper + routes, and focused API tests.
</objective>

<threat_model>
High severity threats block completion:
- T-01 bearer-token disclosure: persisted session rows must not contain directly reusable cookie tokens.
- T-02 stale privilege: role, admin-driven email changes, and deletion must revoke affected sessions predictably.
- T-03 CSRF: authenticated mutating routes must reject missing or invalid CSRF tokens.
</threat_model>

<tasks>
<task id="01-07-01" type="auto">
  <name>Store hashed session tokens and revoke sensitive changes</name>
  <files>src/server/auth/sessions.ts, src/server/auth/current-user.ts, src/server/http/auth-routes.ts, src/server/http/admin-routes.ts, src/server/http/app-flow.test.ts</files>
  <read_first>src/server/auth/sessions.ts, src/server/auth/current-user.ts, src/server/http/auth-routes.ts, src/server/http/admin-routes.ts, src/server/db/schema.ts, .planning/phases/01-auth-profile-and-invite-hardening/01-PATTERNS.md</read_first>
  <action>Add `createSessionId()` and `hashSessionToken(token: string)` in `src/server/auth/sessions.ts` using Node crypto (pattern analog: existing `createSessionToken()` + cookie option centralization in the same file per `01-PATTERNS.md`). New login sessions must store a non-secret `sessions.id` and `sessions.tokenHash = hashSessionToken(rawCookieToken)`. Update `getCurrentSession()` in `src/server/auth/current-user.ts` to look up by `sessions.tokenHash` (pattern analog: current join + `lastSeenAt` update path in `getCurrentSession()` per `01-PATTERNS.md`), while session list/revoke APIs continue using non-secret `sessions.id`. **Legacy-session handling (D-24):** treat any persisted session row with missing `tokenHash` (or equivalent legacy shape) as revoked/invalid and require re-login (do not attempt to keep those sessions working, because the legacy persisted raw token is credential-like). Revoke affected user sessions after admin role changes, admin email changes, and deletion; do not revoke for display-name-only or notifications-only changes. Audit entity IDs must use non-secret session IDs, never raw cookie tokens.</action>
  <acceptance_criteria>
    - `src/server/auth/sessions.ts` exports `createSessionId` and `hashSessionToken`.
    - `src/server/auth/current-user.ts` does not contain `eq(sessions.id, token)`.
    - New login inserts do not store `id: sessionToken`.
    - Tests prove the cookie token is not equal to any persisted session id.
    - Tests prove a legacy session row without `tokenHash` is rejected (requires re-login).
    - Tests prove old agents receive `401 { error: "nicht_angemeldet" }` after role change, admin email change, and deletion.
  </acceptance_criteria>
  <verify><automated>npm test -- --run src/server/http/app-flow.test.ts</automated></verify>
  <done>Raw bearer tokens are not persisted and sensitive account changes invalidate sessions predictably.</done>
</task>

<task id="01-07-02" type="auto">
  <name>Add CSRF baseline for authenticated mutations</name>
  <files>src/server/auth/csrf.ts, src/server/http/auth-routes.ts, src/server/http/admin-routes.ts, src/server/http/app-flow.test.ts</files>
  <read_first>src/server/auth/current-user.ts, src/server/auth/sessions.ts, src/server/http/auth-routes.ts, src/server/http/admin-routes.ts, .planning/phases/01-auth-profile-and-invite-hardening/01-CONTEXT.md, .planning/phases/01-auth-profile-and-invite-hardening/01-PATTERNS.md</read_first>
  <action>Create `src/server/auth/csrf.ts` with `CSRF_HEADER = "x-hermes-csrf"`, `createCsrfToken(sessionId: string)`, `verifyCsrfToken(sessionId: string, token: string | undefined)`, and `requireCsrf(context, request, response)`. Closest integration analogs: cookie/session primitives in `src/server/auth/sessions.ts`, authenticated context in `src/server/auth/current-user.ts`, and router-level middleware patterns in `src/server/http/admin-routes.ts` (`router.use(...)`) per `01-PATTERNS.md`. Use HMAC-SHA256 with `process.env.HERMES_CSRF_SECRET ?? "hermes-dev-csrf-secret"`. Add `GET /api/auth/csrf` for authenticated users. Require the header for authenticated `POST`, `PATCH`, `PUT`, and `DELETE` routes in `auth-routes.ts` and all mutating admin routes. Explicitly document and test the exemption for public unauthenticated `/request-code`, `/verify-code`, and `/register`.</action>
  <acceptance_criteria>
    - `src/server/auth/csrf.ts` exports `CSRF_HEADER`, `createCsrfToken`, `verifyCsrfToken`, and `requireCsrf`.
    - `src/server/http/auth-routes.ts` contains route path `/csrf`.
    - At least one current-user mutation and one admin mutation reject missing CSRF with `403 { error: "csrf_token_ungueltig" }`.
    - Valid CSRF token tests pass for one current-user mutation and one admin mutation.
    - Public login and registration endpoints remain usable without CSRF and have explicit tests.
  </acceptance_criteria>
  <verify><automated>npm test -- --run src/server/http/app-flow.test.ts</automated></verify>
  <done>Authenticated cookie mutations have explicit, tested CSRF enforcement without breaking public auth entry points.</done>
</task>
</tasks>

<verification>
- [ ] `npm test -- --run src/server/http/app-flow.test.ts`
- [ ] `npm run build`
</verification>

<success_criteria>
- Persisted sessions cannot be replayed directly from SQLite/S3 snapshots.
- Authenticated mutations have a consistent CSRF contract.
- Session invalidation behavior is implemented and tested for security-sensitive user changes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-profile-and-invite-hardening/01-07-SUMMARY.md`
</output>

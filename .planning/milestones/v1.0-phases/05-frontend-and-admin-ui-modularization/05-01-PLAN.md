---
phase: 05
plan: 05-01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main.tsx
  - src/client/api/request.ts
  - src/client/api/csrf.ts
  - src/client/errors/errors.ts
  - src/client/types/core.ts
autonomous: true
requirements: [UI-01]
user_setup: []
must_haves:
  truths:
    - Shared client API helpers, DTO types, and user-facing error mapping live outside src/main.tsx.
    - Request behavior (cookies, CSRF attachment rules, error mapping) remains unchanged.
  artifacts:
    - src/client/api/request.ts
    - src/client/errors/errors.ts
    - src/client/types/core.ts
---

<objective>
Extract shared client API helpers, DTO types, and error mapping out of `src/main.tsx`.
</objective>

<tasks>
<task id="05-01-01" type="auto">
  <name>Extract shared types, API request wrapper, CSRF helpers, and error mapping</name>
  <files>src/main.tsx, src/client/api/request.ts, src/client/api/csrf.ts, src/client/errors/errors.ts, src/client/types/core.ts</files>
  <read_first>.planning/phases/05-frontend-and-admin-ui-modularization/05-CONTEXT.md, src/main.tsx</read_first>
  <action>
    Create `src/client/` modules and move the following out of `src/main.tsx` without behavior changes:
    - Shared DTO-like types used across the UI (User, AppSettings, StorageInfo, RestoreDiagnostics, GameEvent, AuditLogEntry, UserSession, InviteCode, RateLimit types)
    - Error mapping (`errorMessages`, `ApiError`, `getErrorMessage`)
    - CSRF helper logic (`getCsrfToken`, `shouldAttachCsrf`, header name)
    - `requestJson` wrapper with the same credentials + JSON parsing behavior

    Ensure `src/main.tsx` continues to compile and exports nothing new; it should import from `src/client/*`.
  </action>
  <verify><automated>npm test && npm run build</automated></verify>
  <done>Core shared client helpers/types are extracted and the app builds/tests cleanly.</done>
</task>
</tasks>

<verification>
- [ ] `npm test`
- [ ] `npm run build`
</verification>

<success_criteria>
- UI-01 is met: shared helpers/types/error mapping live outside `src/main.tsx`.
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-and-admin-ui-modularization/05-01-SUMMARY.md`
</output>


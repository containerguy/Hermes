---
phase: 02-event-and-invite-consistency
plan: 03
type: execute
wave: 2
depends_on:
  - 02-02
files_modified:
  - src/server/http/event-routes.ts
  - src/server/push/push-service.ts
  - src/server/http/event-side-effects.test.ts
  - src/main.tsx
autonomous: true
requirements:
  - EVT-02
  - EVT-03
user_setup: []

must_haves:
  truths:
    - "On successful participation changes, audit log + SSE refresh happen coherently (post-commit), and push behavior remains consistent with status transitions."
    - "On rejected joins due to capacity, there is a best-effort audit entry and an operator-only push attempt; no extra SSE broadcast is forced."
    - "Manual cancel/archive and automatic archive behavior still works after transactional participation refactors."
    - "UI explains event_voll as 'Spieler X von Y' and suggests starting a new round (per D-06) without changing the stable error code."
  artifacts:
    - path: "src/server/http/event-routes.ts"
      provides: "Post-commit side effects and rejection handling around participation updates"
      min_lines: 30
    - path: "src/server/push/push-service.ts"
      provides: "Operator-only push helper for rejection notifications"
      min_lines: 15
    - path: "src/server/http/event-side-effects.test.ts"
      provides: "Automated tests for EVT-02 (audit/push/SSE expectations) and EVT-03 lifecycle regression"
      min_lines: 40
    - path: "src/main.tsx"
      provides: "Event full UX copy 'Spieler X von Y' + suggestion to start new event"
      min_lines: 10
  key_links:
    - from: "src/server/http/event-routes.ts"
      to: "broadcastEventsChanged"
      via: "success-only SSE broadcast"
      pattern: "broadcastEventsChanged\\("
    - from: "src/server/http/event-routes.ts"
      to: "sendPush"
      via: "operator-only push on rejection"
      pattern: "sendPushTo"
---

<objective>
Make participation side effects coherent for both winners and losers (EVT-02) while preserving event lifecycle transitions (EVT-03) and implementing the event-full UX messaging requirement (D-06).

Purpose: Avoid inconsistent realtime/push/audit signals around concurrent participation outcomes and keep archive/cancel + auto-archive stable.
Output: Post-commit side-effect contract, operator-only rejection push, and automated regression tests for EVT-02/EVT-03 plus UI copy improvements for full events.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-event-and-invite-consistency/02-CONTEXT.md
@.planning/phases/02-event-and-invite-consistency/02-RESEARCH.md
@.planning/phases/02-event-and-invite-consistency/02-PATTERNS.md
@.planning/phases/02-event-and-invite-consistency/02-VALIDATION.md

Canonical refs (must read before edits):
- `src/server/http/event-routes.ts`
- `src/server/realtime/event-bus.ts`
- `src/server/audit-log.ts`
- `src/server/push/push-service.ts`
- `src/server/domain/events.ts`
- `src/main.tsx`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement coherent side effects for success vs rejection</name>
  <read_first>
    - src/server/http/event-routes.ts
    - src/server/realtime/event-bus.ts
    - src/server/audit-log.ts
    - src/server/push/push-service.ts
    - .planning/phases/02-event-and-invite-consistency/02-CONTEXT.md
    - .planning/phases/02-event-and-invite-consistency/02-PATTERNS.md
  </read_first>
  <files>src/server/http/event-routes.ts, src/server/push/push-service.ts</files>
  <action>
    Implement EVT-02 per decisions D-10..D-13 (and D-07, D-11 for rejections), without breaking EVT-01 transactional behavior introduced in plan 02-02:

    - Pattern: follow `.planning/phases/02-event-and-invite-consistency/02-PATTERNS.md` (“audit → SSE → conditional push” and “no forced SSE on rejection”), with the key rule: commit first, then side effects.
    - Ensure side effects are emitted only after the participation write has committed (post-commit):
      - Success path: keep or refactor to an explicit “commit first, then side effects” flow so retries never double-emit.
    - Rejection path for capacity (`409 event_voll`):
      - Do NOT call `broadcastEventsChanged(...)` (per D-10).
      - Emit a best-effort audit entry for the rejected attempt (per D-07) with rich metadata:
        - include `eventId`, requested status, prior participation status (if any), `joinedCount` and `maxPlayers` if available, and any useful request context.
      - Emit a best-effort push notification only to admin/manager users (per D-11).
        - Implement a helper in `push-service.ts` to target only operator roles (admin/manager) and keep failures non-blocking.
      - Audit structure is discretion (per D-12): either a new action string or existing action with `outcome: "rejected"` + `reason: "event_voll"` in metadata. Choose one and keep it consistent for both capacity rejections and (if present) other rejections added in this phase.
    - Success path:
      - Keep existing audit + SSE broadcast behavior, but ensure it only happens once per committed update.
      - Keep existing “push on meaningful status transition” semantics; do not add new push fanout to all users for capacity losers.
  </action>
  <acceptance_criteria>
    - On `409 event_voll`, there is no forced SSE broadcast (per D-10), but there is a best-effort audit log entry (per D-07) and an operator-only push attempt (per D-11).
    - Success emits audit + SSE coherently after commit; retries do not duplicate side effects (per D-13).
    - Changes do not modify the stable error code (`event_voll`) for capacity losers.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    Event participation outcomes (success vs capacity rejection) have coherent, post-commit side effects aligned with D-10..D-13.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add automated tests for EVT-02 side effects and EVT-03 lifecycle regression</name>
  <read_first>
    - src/server/http/app-flow.test.ts
    - src/server/http/event-routes.ts
    - src/server/domain/events.ts
    - .planning/phases/02-event-and-invite-consistency/02-VALIDATION.md
  </read_first>
  <files>src/server/http/event-side-effects.test.ts</files>
  <action>
    Create `src/server/http/event-side-effects.test.ts` using the established integration harness patterns:

    EVT-02 assertions (behavioral integration):
    - Capacity rejection emits an audit entry:
      - Force a `409 event_voll` by creating `maxPlayers=1`, having user A join, then having user B attempt join.
      - Query audit logs via any existing admin endpoint used in tests (or directly via DB if tests already do so) and assert the rejection entry exists with reason/outcome metadata.
    - Operator-only push attempt on rejection:
      - Stub/mock the push send function(s) at the module boundary so tests can assert recipients are admin/manager only.
      - Ensure the rejection path triggers exactly one push attempt (not to all users).
    - No forced SSE broadcast on rejection:
      - Spy/stub `broadcastEventsChanged` (or the event-bus send mechanism) and assert it is NOT called on the rejection path.

    EVT-03 assertions (lifecycle regression):
    - Manual cancel/archive routes still work and appear on board after the transactional participation refactor.
    - Auto-archive behavior remains valid:
      - Create an event whose `startsAt` makes it running/expired per existing config, trigger the existing status refresh path (whatever the server uses today), and assert it transitions to archived as expected.
      - The key requirement is “still works after refactor” — use existing helper functions/routes used elsewhere in tests rather than inventing new lifecycle code.
  </action>
  <acceptance_criteria>
    - Tests cover both a success path and a capacity rejection path and assert audit/push/SSE expectations.
    - Tests cover at least one manual lifecycle transition (cancel OR archive) and one automatic/archive-after-window behavior (EVT-03).
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    `npm test` includes EVT-02/EVT-03 regression tests that protect the side-effect contract and lifecycle behavior.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement event-full UX message 'Spieler X von Y' + suggestion</name>
  <read_first>
    - src/main.tsx
    - .planning/phases/02-event-and-invite-consistency/02-CONTEXT.md
  </read_first>
  <files>src/main.tsx</files>
  <action>
    Implement decision D-06 in the UI without changing stable error codes:

    - When the user tries to join and receives `event_voll`, show a message that communicates the “you would be player X of max Y” idea.
    - If the API provides `joinedCount`/`maxPlayers` (per D-09 discretion) use it; otherwise, use the currently displayed event’s `joinedCount`/`maxPlayers` as the source of truth after refreshing.
    - Include a short hint that starting a new round might be appropriate (per D-06), consistent with the existing German copy style.
    - Keep fallback error mapping intact (`event_voll` remains the key).
  </action>
  <acceptance_criteria>
    - UI shows an explicit “Spieler {joinedCount+1} von {maxPlayers}” style message on full-event join attempts (per D-06).
    - Error code remains `event_voll` and existing generic error handling still works.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    The client communicates event capacity rejection clearly and suggests starting a new event/round, while preserving stable API error codes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→API | Untrusted participation inputs and concurrency outcomes cross into side-effect emission. |
| API→SSE/push | External-facing side effects must reflect committed DB state only. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-02-06 | T (Tampering) | Side effects around participation updates | mitigate | Commit-first then side effects; rejections do not broadcast SSE; tests assert correct behavior (EVT-02). |
| T-02-07 | I (Information Disclosure) | Push notifications on rejection | mitigate | Filter recipients to admin/manager only (per D-11); avoid broadcasting rejection details to all users. |
| T-02-08 | R (Repudiation) | Rejected join attempts | mitigate | Best-effort audit log for rejections with reason/outcome metadata (per D-07, D-12). |
| T-02-09 | E (Elevation of Privilege) | Operator-only push targeting | mitigate | Ensure recipient selection uses server-side role checks; do not accept client-provided role claims. |
</threat_model>

<verification>
- `npm test` passes.
- EVT-02 tests assert: rejection has audit + operator-only push + no forced SSE.
- EVT-03 tests assert: cancel/archive and auto-archive still work.
</verification>

<success_criteria>
- EVT-02: Side effects are coherent across success and failure (no “before commit” signals; operator-only push on rejection).
- EVT-03: Manual and automatic lifecycle transitions remain correct after the transactional join changes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-event-and-invite-consistency/02-03-SUMMARY.md`
</output>


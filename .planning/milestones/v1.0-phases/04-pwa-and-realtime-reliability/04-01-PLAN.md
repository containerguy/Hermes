---
phase: 04
plan: 04-01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main.tsx
autonomous: true
requirements: [PWA-01]
user_setup: []
must_haves:
  truths:
    - Notification settings explain secure-context, browser, OS, and (where relevant) PWA-installation limits directly in the UI.
    - The UI detects insecure/unsupported contexts before attempting subscription and provides actionable messaging.
  artifacts:
    - src/main.tsx
  key_links:
    - Phase 4 context D-01..D-03 in 04-CONTEXT.md.
---

<objective>
Add in-product PWA/push limitation messaging and safe client states for unsupported notification contexts.

Purpose: satisfy PWA-01 by making notification expectations honest and actionable inside Hermes.
Output: improved Profile/Notifications UI messaging and a small client-side preflight to avoid confusing failures.
</objective>

<tasks>
<task id="04-01-01" type="auto">
  <name>Add notifications preflight + limitation explainer panel in Profile UI</name>
  <files>src/main.tsx</files>
  <read_first>.planning/phases/04-pwa-and-realtime-reliability/04-CONTEXT.md, .planning/REQUIREMENTS.md, src/main.tsx, public/manifest.webmanifest</read_first>
  <action>
    In `src/main.tsx`, enhance the Profile (logged-in login page) notifications area:

    - Add a small “Voraussetzungen” / “Hinweise” panel near the Notifications buttons that explicitly explains:
      - Secure context requirement (HTTPS or localhost)
      - Browser support requirement (PushManager + Notification + serviceWorker)
      - OS permission requirement (prompt + settings)
      - A short note that smartphone push behavior varies by browser/OS and often works best when the app is installed as a PWA

    - Add a client-side preflight helper (local to `main.tsx`) to compute a `pushSupport` object, e.g.:
      - `isSecureContext`
      - `hasApis` (serviceWorker, PushManager, Notification)
      - `permission` (`default`/`granted`/`denied` when available)
      - derived `canAttemptSubscribe`

    - Update `enableNotifications()` to:
      - Use the preflight result to set a friendlier, specific error message *before* calling server endpoints when the context is unsupported/insecure.
      - Keep existing stable error codes where already used (e.g. `secure_context_erforderlich`, `push_nicht_unterstuetzt`, `permission_abgelehnt`).
      - Avoid calling `navigator.serviceWorker.register` if the preflight says it cannot work (still allow when secure + supported).

    Keep changes behavior-preserving for the happy path: when supported, flow remains `GET /api/push/public-key` -> register SW -> request permission -> subscribe -> POST subscription -> PATCH preferences.
  </action>
  <acceptance_criteria>
    - UI shows explicit limitation messaging (secure context, browser/OS, PWA note) in-product.
    - When running on non-secure LAN HTTP, user sees a clear explanation without a confusing broken flow.
    - Existing notification enable/disable behavior remains functional on supported platforms.
  </acceptance_criteria>
  <verify><automated>npm run build</automated></verify>
  <done>Notification limitations are explained in-product and unsupported contexts are handled cleanly.</done>
</task>
</tasks>

<verification>
- [ ] `npm run build`
</verification>

<success_criteria>
- PWA-01 is met: notification settings explain secure-context, browser, OS, and PWA-installation limits directly in the UI.
</success_criteria>

<output>
After completion, create `.planning/phases/04-pwa-and-realtime-reliability/04-01-SUMMARY.md`
</output>


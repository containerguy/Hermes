---
phase: 06
plan: 06-02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - README.md
  - building.md
  - .env.example
  - Dockerfile
  - docker-compose.yml
  - .github/workflows/docker-image.yml
autonomous: true
requirements: [REL-04, REL-05]
user_setup: []
must_haves:
  truths:
    - Docs and sample env match the production deployment contract for TLS ownership, secure cookies, SMTP, VAPID, S3 creds, single writer, backup verification, and rollback.
    - Docker/compose/CI commands match the documented build/runtime behavior.
---

<objective>
Align docs, `.env.example`, Docker/compose, and CI with the production contract.
</objective>

<tasks>
<task id="06-02-01" type="auto">
  <name>Update operator docs and deployment contract checklist</name>
  <files>README.md, building.md, .env.example, Dockerfile, docker-compose.yml, .github/workflows/docker-image.yml</files>
  <read_first>.planning/phases/06-release-verification-and-documentation/06-CONTEXT.md, README.md, building.md, .env.example, Dockerfile, docker-compose.yml, .github/workflows/docker-image.yml</read_first>
  <action>
    Ensure REL-04/REL-05 are met by tightening docs and examples:
    - Add a short release checklist section covering TLS/reverse proxy ownership, cookie secure flag, SMTP, VAPID, S3 creds sources, single writer warning, backup verification, and rollback/recovery key usage.
    - Ensure `.env.example` reflects required variables and safe defaults (prod vs dev callouts).
    - Confirm Dockerfile and compose reflect env usage and volumes; ensure CI steps match docs and are accurate.
  </action>
  <verify><automated>npm run build</automated></verify>
  <done>Docs/env/Docker/CI match the production contract and build passes.</done>
</task>
</tasks>

<verification>
- [ ] `npm run build`
</verification>

<output>
After completion, create `.planning/phases/06-release-verification-and-documentation/06-02-SUMMARY.md`
</output>


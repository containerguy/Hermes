---
phase: 06
plan: 06-03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [REL-06]
user_setup: []
must_haves:
  truths:
    - Project passes `npm test`, `npm run build`, and `npm audit --omit=dev` (or blockers are explicitly documented).
---

<objective>
Run release validation commands and document any remaining environment-specific blockers.
</objective>

<tasks>
<task id="06-03-01" type="auto">
  <name>Run final validation commands and record outcomes</name>
  <files>.planning/phases/06-release-verification-and-documentation/06-03-SUMMARY.md, .planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>.planning/phases/06-release-verification-and-documentation/06-CONTEXT.md, .planning/REQUIREMENTS.md, .github/workflows/docker-image.yml</read_first>
  <action>
    Run the release gate commands:
    - `npm test`
    - `npm run build`
    - `npm audit --omit=dev`

    If any fail due to environment constraints, capture the exact failure and classify it as an environment-specific blocker in the SUMMARY.
  </action>
  <verify><automated>npm test && npm run build && npm audit --omit=dev</automated></verify>
  <done>Release validation results are recorded and REL-06 is either satisfied or blockers are explicitly documented.</done>
</task>
</tasks>

<verification>
- [ ] `npm test`
- [ ] `npm run build`
- [ ] `npm audit --omit=dev`
</verification>

<output>
After completion, create `.planning/phases/06-release-verification-and-documentation/06-03-SUMMARY.md`
</output>


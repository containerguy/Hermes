---
phase: 06
plan: 06-01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/http/app-flow.test.ts
  - src/server/http/event-side-effects.test.ts
  - src/server/push/push-service-cleanup.test.ts
autonomous: true
requirements: [REL-01, REL-02, REL-03]
user_setup: []
must_haves:
  truths:
    - Focused API tests cover auth throttling, generic login responses, session revocation, invite limits, and concurrent event joins.
    - Storage/admin tests cover restore validation signals and destructive-action authorization.
    - Push tests cover malformed payload handling and failed subscription cleanup.
---

<objective>
Add focused API/storage tests for security, concurrency, restore, push, and destructive admin actions.
</objective>

<tasks>
<task id="06-01-01" type="auto">
  <name>Extend server tests for release-critical coverage</name>
  <files>src/server/http/app-flow.test.ts, src/server/http/event-side-effects.test.ts, src/server/push/push-service-cleanup.test.ts</files>
  <read_first>.planning/phases/06-release-verification-and-documentation/06-CONTEXT.md, .planning/REQUIREMENTS.md, src/server/http/app-flow.test.ts, src/server/http/event-side-effects.test.ts, src/server/push/push-service-cleanup.test.ts</read_first>
  <action>
    Add/extend tests to cover REL-01..REL-03 without requiring external services:
    - Auth: generic login-code response shape, throttling behavior (as already implemented), and session revocation paths.
    - Invites: maxUses/concurrency constraints (should already be in Phase 2) and basic regression assertions.
    - Restore: validation-first behavior and error codes/diagnostics shape for failures (no real S3 required).
    - Admin destructive actions: require admin + CSRF (basic authz regression).
    - Push: ensure failing subscription cleanup remains covered (already exists) and keep tests network-free.
  </action>
  <verify><automated>npm test</automated></verify>
  <done>Release-critical backend behaviors have focused regression tests and `npm test` passes.</done>
</task>
</tasks>

<verification>
- [ ] `npm test`
</verification>

<output>
After completion, create `.planning/phases/06-release-verification-and-documentation/06-01-SUMMARY.md`
</output>

